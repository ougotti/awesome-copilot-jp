name: CI

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v5

      - name: Python セットアップ
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Python 構文チェック
        run: |
          python -m py_compile scripts/*.py
          echo "Python 構文チェック OK"

      - name: JSON 形式チェック
        run: |
          python3 - << 'EOF'
          import json
          with open('scripts/known-files.json') as f:
              json.load(f)
          print('JSON 形式チェック OK')
          EOF

      - name: 内部リンクチェック
        run: python3 scripts/check_links.py

      - name: 件数表記チェック
        run: python3 scripts/check_catalog_counts.py

  build:
    name: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v5

      - name: Python セットアップ
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: 依存関係のインストール
        run: pip install requests

      - name: ファイル構造の確認
        run: |
          test -f README.md || (echo "README.md が存在しません" && exit 1)
          test -f docs/instructions.md || (echo "docs/instructions.md が存在しません" && exit 1)
          test -f docs/agents.md || (echo "docs/agents.md が存在しません" && exit 1)
          test -f docs/prompts.md || (echo "docs/prompts.md が存在しません" && exit 1)
          test -f docs/use-cases.md || (echo "docs/use-cases.md が存在しません" && exit 1)
          test -f docs/office-work.md || (echo "docs/office-work.md が存在しません" && exit 1)
          test -f docs/financial-services.md || (echo "docs/financial-services.md が存在しません" && exit 1)
          test -f scripts/known-files.json || (echo "scripts/known-files.json が存在しません" && exit 1)
          echo "ファイル構造チェック OK"

  test:
    name: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v5

      - name: Python セットアップ
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: 依存関係のインストール
        run: pip install requests

      - name: スクリプトのドライランテスト
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          UPSTREAM_REPO: github/awesome-copilot
          TRACKED_DIRS: instructions,agents,prompts
          TRACKED_FILE: scripts/known-files.json
          DRY_RUN: 'true'
        run: |
          python scripts/check_upstream_updates.py
          echo "スクリプトのドライランテスト OK"